# Choose a file and return its path

Choose a file and return its path

## Usage

``` r
ya_choose_file()
```

## Value

A single string representing the full file path of the chosen file. The
function normalizes the file path to use forward slashes and ensures
that the selected file exists.
